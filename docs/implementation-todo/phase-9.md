# Phase 9: Pi Writing Agent

## Phase overview

- Purpose: add durable Pi agent sessions, the four frozen read tools, three typed mutation proposal tools, approval/application, and the assisted-writing UI.
- Checkpoints: 20–23.
- Current status: Checkpoints 20 through 23 are completed and verified; Phase 9 is complete.

## Superseded design

The pre-freeze plan (nine read tools, six write tools, `agent_messages`/`agent_tool_calls` tables, durable Agent turns, a persistent compaction subsystem) is superseded by the [CP19.5 audit](../audits/2026-07-16-complexity-reduction-and-agent-boundary.md) and [ADR 001](../adrs/001-cp19.5-boundaries.md) and survives only in git history. The frozen surface is:

- Read tools: `get_writing_context`, `read_section`, `search_knowledge`, `read_citations`.
- Proposal tools: `propose_brief_update`, `propose_outline_patch`, `propose_section_patch`.
- Persistence: `agent_sessions`, `agent_runs`, `agent_events`, `mutation_proposals`, plus the existing `model_requests`. No `mutation_applications`, `accepted_source_links`, or `proposal_citations` until real query pressure exists.
- Agent turns are request-scoped: project close aborts the run; no durable-turn restart. History is durable; runs are not.
- No persistent compaction subsystem: recent N events plus, when needed, one bounded summary stored as an ordinary `agent_events` row.

## Codebase starting points (verified before Checkpoint 20; updated after completion on 2026-07-21)

- The `agent-worker` role remains an app-level lazy persistent utility process: entry `src/workers/agent-model.ts` (bundle `agent-worker.js`), Main client `src/main/providers/agent-model-client.ts` over `src/main/workers/persistent-utility-process.ts`. Checkpoint 20 added the capability-bound sessionful run host, serialized persist-before-publish event delivery, steering/follow-up commands, and project-close cancellation. The older CP14 single-call method remains only as a compatibility surface and has no product caller.
- `model_requests` (project migration `0009`) already has a nullable indexed `agent_run_id` forward hook; `ModelExecutionService` (`src/main/providers/model-execution-service.ts`) writes rows. Provider role `agent` (`openai-compatible`, capabilities `chat`/`tool-calling`) and `CredentialService`/`MainPiCredentialStore` are in place.
- Project migration `0016` now owns the strict `agent_sessions`, `agent_runs`, `agent_events`, and `mutation_proposals` tables and their foreign-key/index boundary.
- `ManuscriptService.appendRevision` (`src/main/manuscript/manuscript-service.ts`) rejects agent sources and lineage IDs by design (CP19.7.2); CP22 adds the authorized application path. The Zod contracts already admit `source: 'agent'`, `sourceClass: 'agent_accepted'`, and the three lineage IDs.
- Editor selection context (`activeSectionId`, `activeBlockId`, `selectedBlockIds`) is renderer memory only (`writing-workspace.tsx`, `SectionEditor.onSelectionContextChange`); the run-start IPC payload carries it as a validated snapshot. Main tracks the active section per session in `src/main/ipc/editor-ipc.ts`.
- Checkpoint 22 added the session-revocable Main→renderer section-changed push in `src/main/ipc/agent-mutation-ipc.ts`; the diagnostics channel remains the precedent for the broader Checkpoint 23 Agent-event subscription.
- `@earendil-works/pi-agent-core` and `@earendil-works/pi-ai` are exactly pinned to 0.80.10. The 0.80.8–0.80.10 changelog and installed type surface were characterized before upgrading: no core `Agent` lifecycle changes affect this design; the relevant 0.80.8 changes are in pi-ai authentication/model handling. The pinned API has no `createSession` and no session-schema `version` field, so session state and compatibility versioning remain app-owned in `project.sqlite`.
- Re-verified 2026-07-21 against the installed 0.80.10 type surface: the `Agent` class exposes `initialState`, `transformContext`, `beforeToolCall`, `steeringMode`/`followUpMode`, `steer`/`followUp`, `subscribe`, `abort`, and `waitForIdle` exactly as D1 assumes; JSONL session storage lives only under the harness (`harness/session/`), which D1 rejects. The active boundary is `AgentSessionRuntime` plus the embedding/rerank gateways in `src/main/providers/gateways.ts`, with strict Agent envelopes in `src/shared/contracts/agent.ts`.

## Design decisions for this phase

- D1. Use the low-level `Agent` class, not `AgentHarness`. The harness brings tree-based JSONL session storage and its own compaction, which would duplicate durable state outside `project.sqlite` and conflict with the freeze. CP20 uses `initialState.messages` rebuilt from `agent_events`, `transformContext` for bounded context, `beforeToolCall` for preflight blocking, `steeringMode`/`followUpMode` for steering/follow-up, and `subscribe` for the event stream.
- D2. The agent-worker stays an app-level lazy singleton (the CP19.5.6 wiring), hosting at most one active run per open project. Runs carry the `projectSessionId` capability; project close aborts in-flight runs through the existing cancel propagation. The pre-freeze "launch a dedicated process only while a project is open" item is adjusted to this existing lifecycle — no per-project process churn.
- D3. The tool bridge is a dedicated MessagePort, distinct from streaming and logging traffic (architecture.md "Agent tool bridge"). The worker initiates `AgentToolRequest` envelopes (`requestId`, `projectSessionId`, `agentSessionId`, `agentRunId`, `toolCallId`, `modelRequestId`, `toolName`, `args`); Main validates the envelope and active session, validates args against the domain contract, authorizes policy, executes, persists tool lifecycle into `agent_events`, and returns a bounded structured result. Tool errors are structured errors, never successful text.
- D4. Dual schema per tool: a TypeBox model-facing parameter schema (Pi requirement — verified 2026-07-21: 0.80.10 tools are `AgentTool<TParameters extends TSchema>` from the `typebox` v1 package) and the existing Zod domain contract as Main-side authority. A contract test asserts field/bound parity; no codegen layer. Checkpoint 21 added the exact direct `typebox` 1.1.38 pin matching Pi rather than importing its transitive copy implicitly.
- D5. Credentials: Main resolves the provider credential and passes the API key in the run-start envelope (worker memory only, as the current probe does). Per Pi model call, the worker reports usage/retry metadata and Main writes the `model_requests` row with `agent_run_id`. Initial, steering, and follow-up calls carry a pre-authorized model-request ID; after a tool batch, the worker requests the next call through the capability-bound run protocol, Main creates the row, and only then authorizes that provider call. `model_requests` authority stays in Main.
- D6. Session continuity: a new run rebuilds model messages from the session's `agent_events` (recent N within token budget, plus the latest `compaction_summary` row when present). Retry/continue after interruption is a new run on the same session, never a resumed worker process.
- D7. Compatibility: `agent_sessions` stores the Pi runtime version and the app event-schema version. On mismatch, existing history stays readable but new runs require a new session.
- D8. Application path (CP22): a Main-owned service mints `agent_accepted` revisions with full lineage (run, tool call, proposal, base revision, model request, cited blocks), revalidates `baseRevisionId`/base outline version inside the apply transaction, writes revisions + outline mutations atomically per proposal, materializes, then pushes a section-changed notification. The renderer flushes any pending save for the target section before sending approval; races after that are caught by the apply-time revalidation and surface through the existing conflict-preserving draft path — no silent overwrite, no blocking flush handshake.
- D9. Undo is scoped to an applied `section_patch` and is a user-initiated new revision restoring the pre-agent parent content (`manual_checkpoint` class, linked from the proposal through `undo_revision_id`); history is never rewritten. Brief and outline applications remain versioned and traceable but are not part of this first section-revision undo surface.

## Checkpoint 20: Agent Session Runtime, Run Lifecycle, And Durable Event Trace

- [x] 20.1 Review the 0.80.7 → current Pi changelog, re-characterize the API surface used here (`Agent`, events, `transformContext`, `beforeToolCall`, steering/follow-up, abort), decide the exact pin, and record the characterization as done in Checkpoint 14.
- [x] 20.2 Add project migration `0016`: `agent_sessions` (Pi runtime version, event-schema version, status, timestamps), `agent_runs` (session FK, status `running|completed|interrupted|failed`, provider/model fingerprints, editor-context snapshot JSON, error JSON, timestamps), `agent_events` (session FK, monotonic `sequence`, type in `user_message|assistant_message|tool_call|tool_result|run_interrupted|run_completed|compaction_summary` — the freeze audit's six types plus `compaction_summary`, which that audit's summary-as-ordinary-event rule explicitly permits, bounded payload JSON, nullable `model_request_id` link), and `mutation_proposals` (session/run/tool-call FKs, kind, payload JSON, base revision/outline refs, status `pending|approved|rejected|applied|failed|undone`, `decision_at`, `applied_revision_id`, `rejected_reason`). STRICT tables, foreign keys, and indexes on session+sequence, run, and status.
- [x] 20.3 Replace the production probe path with a sessionful run host on the low-level `Agent` class per D1 (no tools in this checkpoint), while retaining the CP14 single-call method only as an unused compatibility surface; replace the process-lifetime retry counter with per-call attempt counting restored in the custom `streamFn` completion path. Pi-ai 0.80.7 through 0.80.10 expose no retry-count hook, so the scoped fetch count is the characterized fallback while Agent model calls remain sequential and the worker hosts at most one active run.
- [x] 20.4 Define the run protocol on the existing persistent-utility channel: `run_start` (projectSessionId, session/run IDs, rebuilt messages, model config, credential), streamed normalized events, abort via existing cancel propagation, and terminal run state. Reject stale `projectSessionId`/run IDs per the CP19.7.6 envelope rules.
- [x] 20.5 Add a Main-owned `AgentSessionService`: session create/list, run start (persist session/run rows and the `user_message` event before starting the worker stream), worker-event → `agent_events` mapping with persist-before-render ordering (Main persists each event, then forwards it to the renderer), abort (persist partial assistant output with an interrupted flag, then `run_interrupted`; never mark interrupted output complete), and crash handling (worker exit mid-run marks the run interrupted/failed with the original `err` logged).
- [x] 20.6 Record one `model_requests` row per Pi model call with `agent_run_id` set, from worker-reported usage/retry metadata (D5).
- [x] 20.7 Support steering and follow-up queues at the runtime level (`steer`/`followUp` while running; queued while a run is active) with explicit persisted state in `agent_events`.
- [x] 20.8 Emit structured lifecycle logs at session/run boundaries with `operationId`/`agentRunId` correlation through the production ALS context, and aggregate agent-worker logs through the existing port-logger/log-collector path (CP19.6.6).
- [x] 20.9 Test with a scripted mock `streamFn`: event ordering, abort mid-stream, renderer closure, worker crash mid-run, project close revocation, reopen history accuracy, steering/follow-up state, and mock-provider streaming/retry/auth/redaction through the CP14 fixture pattern.

Acceptance criteria: an agent conversation is project-local and durable in `agent_events`; renderer, worker, or project interruption cannot create a falsely complete answer; project close revokes the run and a later reopen shows accurate history; every model call is linked in `model_requests`.

Verification (2026-07-21): `pnpm check` passed 281 files with only the existing generated shadcn `document.cookie` warning; Node/web `pnpm typecheck` passed; canonical Electron-hosted `pnpm test` passed 78 files/353 tests; `pnpm build` emitted the production Agent worker; `pnpm build:unpack` passed for the signed macOS arm64 unpacked app; `pnpm test:e2e` passed 11/11; packaged hybrid smoke passed native search, provider-fallback, and stale-session scenarios; and `git diff --check` passed. Focused Agent coverage includes strict migration/index checks, capability-bound contracts, durable ordering, per-call model-request linkage, history rebuild, steering/follow-up, retry metadata, credential redaction, partial abort, project close, renderer delivery failure, worker crash, relaunch recovery, and version incompatibility. Checkpoint 21 remains unstarted.

## Checkpoint 21: Context Builder, Tool Bridge, And The Four Read Tools

- [x] 21.1 Implement the Main-owned `ContextBuilder`: brief, outline with objectives/statuses/counts, active section summary, the validated renderer editor-context snapshot (architecture.md "Initial read tools" — the UI injects active editor context at run start rather than the Agent fetching high-frequency cursor state), the user request, prior events within token budget via Pi's per-provider-call `transformContext`, tool descriptions, and the safety policy that delimits retrieved knowledge as untrusted content. Reuse the authoritative `ManuscriptService.getWorkspace`/`getSection` data and `extractSectionText`; do not duplicate manuscript queries. The `ContextBuilder` is real assembly logic with testable invariants inside the Main agent module area (`src/main/agent/`), not a revival of the superseded standalone `context-builder.ts` wrapper named in the CP19.5 audit's module rule.
- [x] 21.2 Establish the dedicated tool-bridge MessagePort and `AgentToolRequest` handling per D3, including bounded results and tool lifecycle persistence. Keep model-call authorization on the existing capability-bound persistent-utility run protocol rather than multiplexing it onto the tool port: when a completed tool batch causes Pi to request another provider call and no pre-authorized ID exists, the worker emits a bounded call request, Main first creates the linked `model_requests` row, and the worker proceeds only after receiving the matching authorization.
- [x] 21.3 Add an exact direct `typebox` 1.1.38 pin matching Pi 0.80.10, then register the four frozen read tools in the worker with TypeBox parameter schemas, backed by Agent-specific Zod domain contracts in `src/shared/contracts` (D4; model arguments never include `projectSessionId`): `get_writing_context({ includeBrief, includeOutline, activeSectionId? })`, `read_section({ sectionId, blockIds?, cursor?, limit? })` (covers block reads), `search_knowledge` (bounded `RetrievalService.search` with Main injecting the active capability), and `read_citations` (bounded `RetrievalService.expand`).
- [x] 21.4 Enforce result-count, text-size, and pagination limits on every tool; use a revision-bound opaque cursor for `read_section` so pagination cannot silently cross a manuscript revision; permit parallel execution only among independent read tools.
- [x] 21.5 Delimit retrieved knowledge as untrusted source content in both context assembly and tool results; it cannot alter system or tool policy.
- [x] 21.6 Persist citation IDs and tool provenance in the `agent_events` payloads.
- [x] 21.7 Test prompt-injection fixtures, stale project sessions, unauthorized tool names, malformed arguments, oversized results, revision changes between paginated section reads, parallel read ordering, source deletion during a run, bridge messages with revoked sessions, and the automatic post-tool provider call receiving exactly one Main-authorized linked `model_requests` row before it starts.

Acceptance criteria: the agent can ground itself in the brief, outline, active section, and relevant knowledge with citations through bounded tools only; it has no generic project, filesystem, SQL, or operating-system access.

Preimplementation audit (2026-07-21; no 21.x code had started at audit time): the authoritative manuscript surface is `ManuscriptService.getWorkspace`/`getSection` plus `extractSectionText`; retrieval already supplies bounded `search` and 20-ID `expand` contracts; Pi 0.80.10 tools require TypeBox `TSchema`, throw on errors, expose validated `beforeToolCall` arguments, and support per-tool `parallel` execution; Electron 43 `MessagePortMain` supports a dedicated transferable request/response port. The audit added the automatic post-tool model-call authorization handshake above because CP20's initial/steering/follow-up IDs cannot cover Pi's tool-loop continuation call.

Verification (2026-07-21): local `npm run check` passed 288 files with only the existing generated shadcn `document.cookie` warning; `npm run typecheck` passed the Node and renderer projects; the canonical Electron runner `node scripts/run-tests.mjs` passed 81 files/370 tests; `npm run build` emitted `agent-worker.js` and its Agent tool-contract chunk; the signed macOS arm64 `npm run build:unpack` passed; `npm run test:e2e` passed 11/11 outside the sandbox; packaged hybrid smoke passed native search, provider-failure fallback, and stale-session scenarios; the packaged asar contains both `out/main/agent-worker.js` and the Agent tool chunk; an isolated real-app launch produced an integrity-valid `app.sqlite` with application ID 1464615248, user version 1, and schema manifest version 1; and `git diff --check` passed. The `pnpm` executable hung before starting commands in this environment, so the repository-documented npm/direct-runner equivalents were used and no `pnpm` pass is claimed. Focused coverage proves bounded context assembly and fail-closed context budgeting, prompt-injection policy order, exact four-tool TypeBox/Zod surfaces, malformed/unauthorized/oversized rejection, revision-bound section pagination, source deletion reporting, reverse-order parallel responses, port and project-capability revocation, citation/provenance persistence, reopened history without orphaned tool-use messages, a real Pi tool loop, and exactly one durably linked Main-authorized model request before the automatic post-tool call. Checkpoint 22 remains unstarted pending explicit approval.

## Checkpoint 22: Typed Mutation Proposals, Preview, Approval, And Application

Preimplementation audit (2026-07-21; no 22.x code had started): the current source supports the planned pure BlockNote simulator, editor flush, materialization, outline/brief CAS, transferable tool bridge, and Main-to-renderer push pattern. Migration `0016` requires `applied_revision_id` for every `applied`/`undone` proposal even though brief and outline changes do not necessarily create section revisions, and it has no place to link the D9 undo revision. D9 is specifically revision-based section undo, while full outline-delete undo is not supported by the current hard-delete data model. On 2026-07-21 the user approved the recommended resolution: a forward-only `0017` rebuild of `mutation_proposals` with kind-specific base/result constraints, `applied_brief_version`, `applied_outline_version`, and `undo_revision_id`, with undo scoped to `section_patch`; this preserves the frozen table surface. The broader reversible-outline/soft-delete alternative remains outside the approved checkpoint.

- [x] 22.1 Add forward-only migration `0017` per the approved schema resolution, then define versioned Zod mutation payload contracts in `src/shared/contracts`: `BriefUpdate`, `OutlinePatch` (section create, metadata update, move/reorder, delete), and `SectionPatch`. `SectionPatch` implements the typed BlockNote operation set sketched in architecture.md "Block mutations" (`insertBlocks`/`updateBlock`/`removeBlocks`/`replaceBlocks`/`moveBlocks`) — no block-mutation contracts exist in code today, so this checkpoint creates them; the brief and outline domain operations already exist in `ManuscriptService` (`updateBrief` with `brief_version_conflict`, `createSection`/`updateSection`/`moveSection`/`deleteSection` with outline-version checks). Every proposal requires target IDs plus `baseRevisionId`/base brief or outline version.
- [x] 22.2 Register the three proposal tools as sequential write tools (Pi `toolExecution` plus Main policy); block disallowed or oversized calls through `beforeToolCall` preflight and Main policy.
- [x] 22.3 Persist the `mutation_proposals` row before returning tool success; the tool result carries the proposal ID and a structured preview payload (affected sections/blocks, before/after text, cited sources).
- [x] 22.4 Build a pure validator/simulator module (no I/O) that applies block operations to native BlockNote JSON without committing; validate block IDs, schema, nesting, content size, operation count, and target existence. Main uses it for validation and preview generation.
- [x] 22.5 Add session-authorized approve/reject IPC. Rejection records the decision without touching manuscript state. Approval revalidates the base refs against current revisions inside the apply transaction (stale proposals fail closed).
- [x] 22.6 Implement the Main-owned application path per D8: atomic per-proposal application (all operations or none), `agent_accepted` revisions with lineage, materialization, and a section-changed push so the active editor reloads from Main authority; renderer flushes pending saves for the target section before sending approval.
- [x] 22.7 Implement undo per D9 and track `undone` proposal state.
- [x] 22.8 Test stale proposal rejection, concurrent manual edit during preview, missing block, invalid nesting, duplicate IDs, partial multi-operation failure atomicity, approve-after-project-switch, reject, undo, crash after proposal but before apply, and renderer-asserted `agent_accepted` source clamping (adversarial, extending CP19.7.2).

Acceptance criteria: the agent cannot bypass user/policy approval or revision checks; every accepted change is atomic, visible, and fully traceable through revisions or version results, proposals, and model requests; accepted section edits are undoable through a new linked revision; stale proposals never overwrite newer manual work.

Completion evidence (2026-07-21): migration `0017` enforces kind-specific base/result and undo links without adding a table; the seven-tool bridge exposes only four parallel reads plus three sequential proposal tools; Main persists previews before tool success and authorizes decisions with the active project session; accepted section revisions carry run/tool/proposal lineage and publish authoritative reload events; brief/outline applications record their resulting versions; section undo appends a linked manual-checkpoint revision only while the Agent revision is current. Adversarial coverage includes stale/manual races, invalid/missing/duplicate/deep blocks, multi-operation atomicity, unsupported citations, stale project capabilities, reject, restart-before-apply recovery, successful and stale undo, schema CHECK failures, and editor-flush-before-approve ordering. The final gate passed local Biome across 300 files (only the existing generated shadcn cookie warning), Node/web typecheck, canonical Electron Vitest (86 files/388 tests), production build, signed macOS arm64 unpack and signature inspection, Electron Playwright (11/11), packaged hybrid native smoke, packaged asar/native-module inspection, isolated real-app `app.sqlite` integrity/schema verification, and `git diff --check`.

## Checkpoint 23: Agent Writing UI, Compaction, And End-To-End Workflow

Preimplementation audit (2026-07-21; no 23.x product code has started): the renderer still contains only the placeholder Agent `Sheet` and unavailable sidebar label, while selection context is already available in renderer memory. `AgentSessionService` has create/list/event/start/steer/follow-up/abort methods, but production exposes none through IPC, does not wire its optional event publisher, keeps live assistant deltas only in Main memory, and returns only the first 1,000 durable events without a replay cursor. Proposal tool-result events retain their original `pending` payload, so reopened UI must overlay current state from `mutation_proposals`; no session proposal-list API exists yet. The worker's current `transformContext` is a byte-based recent-turn bound that may throw, ignores `compaction_summary`, and hard-codes reasoning off. Existing E2E fixtures can host loopback MinerU and OpenAI-compatible mock servers, so 23.7 does not require a new dependency or production test backdoor.

Approved implementation semantics (2026-07-21):

- Keep thinking visibility to a truthful generic activity state (for example, “Working…”) and never expose or synthesize chain-of-thought. The initial worker has reasoning disabled and no durable thinking contract; provider reasoning controls remain deferred rather than being silently added in the UI checkpoint.
- Add one Main-owned, project-session-revocable Agent event broker. A renderer subscription installs its lease before replay, reads bounded sequence-cursor pages from `agent_events`, queues live durable events during replay, and de-duplicates by sequence. Assistant text deltas are explicitly ephemeral; terminal assistant/tool/run events are durable before publish, and closing the panel only drops the subscription.
- Add a session-scoped proposal query that reads current rows from `mutation_proposals`; UI state is derived by joining durable tool events with those authoritative records, so approve/reject/failed/applied/undone state survives close and reopen.
- Treat retry and continue as new immutable runs in the same compatible session, each with a visible persisted user message. Retry repeats the selected prior prompt; continue sends an explicit visible continuation prompt. Stop aborts only the active request-scoped run, while steer/follow-up remain active-run controls.
- Define “real pressure” as the next provider call requiring at least one complete historical turn to be omitted after reserving system/tool/current-turn/output capacity. Use a deterministic token estimator with CJK/adversarial coverage. At most one session summary is generated in this checkpoint, from bounded raw durable events only (never from a prior summary), through `ModelExecutionService` so its request is recorded; its `compaction_summary` payload records the covered sequence. Full events remain authoritative. The non-throwing `transformContext` then injects that summary plus post-summary complete turns and token-truncates by whole turn. Additional/replacement or multi-level summaries remain deferred.

- [x] 23.1 Replace the placeholder agent `Sheet` in the writing workspace (⌘/Ctrl+J) with the real panel composed from the established shadcn shell: session list, message streaming, thinking visibility policy, stop, retry/continue, and steering/follow-up controls.
- [x] 23.2 Add the Main→renderer agent event channel (following the diagnostics push pattern) with session-authorized, replayable-from-`agent_events` semantics; renderer closure never changes run truth.
- [x] 23.3 Render tool calls as structured cards (status, bounded arguments, results, errors, citations) and proposals with section/block diff, source citations, and approve/reject/undo state.
- [x] 23.4 Show active model/provider, token usage, interruption, and retry state without exposing secrets.
- [x] 23.5 Allow starting a run from the current selection, the active section, or the project overview; the renderer injects the editor-context snapshot at run start.
- [x] 23.6 Implement single-level compaction: token-budgeted `transformContext` truncation per run, and when pressure is real, one bounded summary produced through a `ModelExecutionService`-recorded call and stored as a `compaction_summary` event. Full history stays in `agent_events`; summaries never become manuscript or source authority. No multi-level summaries until measured token pressure requires them.
- [x] 23.7 Add the E2E scenario with the mock provider: create project, write brief/outline, import a source, complete MinerU/indexing, ask the agent for evidence, receive citations, receive a section-edit proposal, preview, approve, verify the new revision's lineage and citations, watch the editor reload, close, reopen to accurate history, and undo.

Acceptance criteria: the complete assisted-writing workflow is understandable and recoverable; tool and mutation states remain accurate across cancellation, close, reopen, and compaction.

Completion evidence (2026-07-21): the writing workspace now exposes a real shadcn Agent panel with compatible session selection, replay plus ephemeral streaming, generic activity only, provider/model and usage/retry metadata, stop, retry/continue, steer/follow-up, all three context scopes, structured bounded tool cards, citations, proposal diff/source/state cards, and approve/reject/undo actions. Main owns project-session-authorized Agent IPC and a lease-before-replay broker; cursor pages, queued live delivery, sequence de-duplication, authoritative proposal queries, and delivery-failure isolation preserve durable truth across panel/project closure. The non-throwing deterministic token transform keeps recent whole turns with CJK/adversarial coverage; real historical pressure may create at most one bounded raw-event `compaction_summary` through a recorded `ModelExecutionService` request, while all original events remain authoritative.

The final loopback E2E creates a project, writes its brief/outline/section, imports a PDF, completes MinerU normalization and indexing, executes the real Pi search→citation→section-proposal→final-answer tool loop, previews and approves the proposal, verifies editor reload plus run/tool/proposal/revision/citation lineage, closes and reopens the panel and project to authoritative history, undoes through a linked revision, then stops a streamed run and verifies the durable interrupted state after panel reopen. Final gates passed: local Biome checked 312 files with only the existing generated shadcn `document.cookie` warning; Node/web TypeScript passed; canonical Electron Vitest passed 91 files/402 tests; production build and signed macOS arm64 unpack passed; Electron Playwright passed 12/12; packaged hybrid native/provider-fallback/stale-session smoke passed; strict signature and packaged asar/native-module inspection passed (`out/main/agent-worker.js`, arm64 `better_sqlite3.node`, and `vec0.dylib`, Electron ABI 148); an isolated real-app launch produced an integrity-valid `app.sqlite` with application ID 1464615248, user/schema version 1, and a valid schema manifest row; and `git diff --check` passed.

Post-completion field correction (2026-07-21): a real DeepSeek V4 Flash session proved that Brief approval reached `agent.mutation.applied` but did not invalidate the renderer's manuscript-workspace query, and that outline proposal attempts reached `agent.mutation.propose_failed` with `MutationProposalError(stale_base)` because neither the initial project context nor `get_writing_context` returned `outlineVersion`. Checkpoint 23 now refreshes manuscript state after every successful approve/undo, returns the authoritative outline version alongside `brief.version`, explicitly tells tools which base versions to use, and maps a stale proposal base to a retryable `conflict` with one refresh-and-retry instruction. Regression evidence is 91 Electron-hosted Vitest files/403 tests, successful Node/web typecheck and production build, Biome across 312 files with only the existing generated shadcn warning, `git diff --check`, and 12/12 Electron Playwright scenarios. The expanded loopback Agent E2E asserts the provider receives `outlineVersion`, then approves a Brief update and an Outline patch and verifies both are immediately visible in the renderer. Phase 9 remains complete; this correction does not start Checkpoint 24.

Post-completion field correction (2026-07-21): two real DeepSeek V4 Flash runs repeatedly tried to repair `createSection.sectionId` UUIDs but produced no durable `tool_call`/`tool_result` between assistant messages, confirming that Pi's model-facing UUID schema rejected calls before execution and led the user to stop the stalled run. The outline proposal tool no longer asks the model to mint internal IDs: `createSection.sectionId` is a bounded local reference, and Pi's pre-validation `prepareArguments` hook allocates UUIDs in the Agent worker and consistently rewrites same-patch parent and operation references. The worker-to-Main Zod contract still requires UUIDs, so the trusted persistence boundary is unchanged. Regression evidence is 92 Electron-hosted Vitest files/407 tests, successful Node/web typecheck and production build, Biome across 314 files with only the existing generated shadcn warning, `git diff --check`, 12/12 Electron Playwright scenarios including a non-UUID outline create reference through the real Pi loop, and an isolated integrity-valid app database. Phase 9 remains complete; this correction does not start Checkpoint 24.

## Deferred beyond this phase

Manuscript full-text search as a tool (folded into `read_section` pagination until evidence requires it), automatic application of low-risk proposals, multiple agents/sub-agents, long-term memory, `proposal_citations`/`accepted_source_links` tables, multi-level summaries, Pi harness JSONL session storage, and per-project agent-worker processes.
