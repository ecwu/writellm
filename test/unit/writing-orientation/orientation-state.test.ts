import { expect,test } from 'bun:test'; import { applySave,content,createDraftItem,initializeOrientation,isDirty,updateMotivation } from '../../../src/renderer/features/writing-orientation/orientation-state'; import { emptyDocument } from '../../fixtures/writing-orientation/orientation-fixtures';
test('baseline/draft preserves in-flight edits',()=>{let state=initializeOrientation(emptyDocument());state=updateMotivation(state,'problem','A');const submitted=content(state.draft);state=updateMotivation(state,'problem','B');state=applySave(state,submitted,{document:{...emptyDocument(),revision:1,motivation:{...emptyDocument().motivation,problem:'A'}},createdItemIds:[]});expect(state.saveState).toBe('dirty');expect(state.draft.motivation.problem).toBe('B');}); test('new item defaults and empty reopen selection',()=>{const state=createDraftItem(initializeOrientation(emptyDocument()),crypto.randomUUID());expect(isDirty(state)).toBeTrue();expect(state.draft.outlineItems[0].status).toBe('not-started');expect(initializeOrientation(emptyDocument()).selectedOutlineItemId).toBeNull();});
