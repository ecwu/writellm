import { expect,test } from 'bun:test'; import { readFile } from 'node:fs/promises';
test('interactive primitives include native selection and dialog dismissal/focus return contracts',async()=>{const select=await readFile('src/renderer/components/ui/select.tsx','utf8');const dialog=await readFile('src/renderer/components/ui/dialog.tsx','utf8');const tooltip=await readFile('src/renderer/components/ui/tooltip.tsx','utf8');expect(select).toContain('<select');expect(dialog).toContain("e.key==='Escape'");expect(dialog).toContain('trigger.focus()');expect(dialog).toContain('aria-modal="true"');expect(tooltip).toContain('role="tooltip"');expect(tooltip).toContain('onFocus');});
