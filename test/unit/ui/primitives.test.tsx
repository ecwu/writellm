import { expect,test } from 'bun:test'; import { renderToStaticMarkup } from 'react-dom/server'; import { Button } from '../../../src/renderer/components/ui/button'; import { Input } from '../../../src/renderer/components/ui/input'; import { Select } from '../../../src/renderer/components/ui/select';
test('native primitives preserve roles, names, disabled and invalid state',()=>{const html=renderToStaticMarkup(<><Button disabled>Save</Button><Input aria-label="Title" aria-invalid/><Select aria-label="Theme"><option>System</option></Select></>);expect(html).toContain('<button');expect(html).toContain('disabled');expect(html).toContain('aria-invalid="true"');expect(html).toContain('<select');});
