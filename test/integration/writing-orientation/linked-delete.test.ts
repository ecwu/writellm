import { expect,test } from 'bun:test'; import { readFile } from 'node:fs/promises'; test('linked delete is an authoritative main-owned refusal',async()=>{const source=await readFile('src/main/writing-orientation/repository.ts','utf8');expect(source).toContain("code: 'LINKED_DELETE_NOT_AVAILABLE'");expect(source.indexOf('if (item.chapterRef)')).toBeLessThan(source.indexOf('writeAndCommit(session, document)',source.indexOf('deleteOutlineItem')));});
