import { expect,test } from 'bun:test'; import { moveItem } from '../../../src/renderer/features/writing-orientation/reorder'; test('100 repeated reorder cycles produce no duplicates or wrong order',()=>{const original=Array.from({length:20},(_,index)=>`item-${index}`);let items=[...original];for(let index=0;index<100;index++){items=moveItem(items,0,items.length-1);items=moveItem(items,items.length-1,0);expect(new Set(items).size).toBe(items.length);}expect(items).toEqual(original);});
