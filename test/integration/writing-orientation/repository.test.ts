import { expect,test } from 'bun:test'; import { mkdtemp,mkdir,readFile,writeFile } from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import { WritingOrientationRepository } from '../../../src/main/writing-orientation/repository'; import { ORIENTATION_KIND } from '../../../src/shared/writing-orientation';
test('repository saves, reopens, deduplicates and rejects stale revisions',async()=>{const root=await mkdtemp(path.join(os.tmpdir(),'orientation-'));await mkdir(path.join(root,'workspace'));const session={projectId:crypto.randomUUID(),projectRoot:root,sessionId:crypto.randomUUID()},commits:number[]=[];const repo=new WritingOrientationRepository({commitContent:async(_r:string,_f:string,revision:number)=>{commits.push(revision);}} as any,()=> '2026-07-12T00:00:00.000Z');const mutationId=crypto.randomUUID(),input={baseRevision:0,mutationId,motivation:{problem:'p',targetReaders:'',desiredOutcome:''},outlineItems:[]};const first=await repo.save(session,input);expect(first.ok).toBeTrue();expect(await repo.save(session,input)).toEqual(first);expect(commits).toEqual([1]);const stale=await repo.save(session,{...input,mutationId:crypto.randomUUID()});expect(stale.ok?'':stale.error.code).toBe('REVISION_CONFLICT');expect(JSON.parse(await readFile(path.join(root,'workspace','writing-orientation.json'),'utf8')).revision).toBe(1);});
test('authoritative linked item refuses deletion with zero write',async()=>{const root=await mkdtemp(path.join(os.tmpdir(),'orientation-linked-'));await mkdir(path.join(root,'workspace'));const session={projectId:crypto.randomUUID(),projectRoot:root,sessionId:crypto.randomUUID()},outlineItemId=crypto.randomUUID();const document={kind:ORIENTATION_KIND,schemaVersion:1 as const,projectId:session.projectId,revision:4,updatedAt:'2026-07-12T00:00:00.000Z',motivation:{problem:'',targetReaders:'',desiredOutcome:''},outlineItems:[{outlineItemId,title:'Linked',summary:'',status:'in-progress' as const,chapterRef:crypto.randomUUID()}]};await writeFile(path.join(root,'workspace','writing-orientation.json'),JSON.stringify(document));let writes=0;const repo=new WritingOrientationRepository({commitContent:async()=>{writes++;}} as any);const result=await repo.deleteOutlineItem(session,{outlineItemId,baseRevision:4,mutationId:crypto.randomUUID()});expect(result.ok?'':result.error.code).toBe('LINKED_DELETE_NOT_AVAILABLE');expect(writes).toBe(0);expect(JSON.parse(await readFile(path.join(root,'workspace','writing-orientation.json'),'utf8'))).toEqual(document);});
