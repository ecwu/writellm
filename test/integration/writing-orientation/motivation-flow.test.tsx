import { expect,test } from 'bun:test'; import { readFile } from 'node:fs/promises'; test('motivation flow exposes explicit save, empty guidance, retry and shortcut',async()=>{const source=await readFile('src/renderer/features/writing-orientation/WritingOrientationPanel.tsx','utf8');for(const term of ['Problem to solve','Not filled in yet','Retry','metaKey','ctrlKey','Unsaved changes'])expect(source).toContain(term);});
