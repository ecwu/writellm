import { expect,test } from 'bun:test'; import { readFile } from 'node:fs/promises'; test('dedicated fixture exposes portal, Escape, tooltip and fallback-focus cases',async()=>{const dialog=await readFile('src/renderer/components/ui/dialog.tsx','utf8');const tooltip=await readFile('src/renderer/components/ui/tooltip.tsx','utf8');expect(dialog).toContain('createPortal');expect(dialog).toContain("e.key==='Escape'");expect(dialog).toContain('trigger?.isConnected');expect(tooltip).toContain('onMouseEnter');});
