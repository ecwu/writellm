import { expect,test } from 'bun:test';import { readFile } from 'node:fs/promises';
test('provider runtime preserves sandbox, exact preload namespace and project isolation',async()=>{const [main,preload,shared]=await Promise.all(['src/main/main.ts','src/preload/preload.cts','src/shared/provider-settings.ts'].map(x=>readFile(x,'utf8')));expect(main).toContain('contextIsolation: true');expect(main).toContain('nodeIntegration: false');expect(main).toContain('sandbox: true');expect(preload).toContain("exposeInMainWorld('writellmProviderSettings'");expect(shared).not.toMatch(/project(Id|Path)/);});
