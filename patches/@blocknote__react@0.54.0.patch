diff --git a/dist/confirmDiscardUnsavedComment-D1Q3ha7c.js b/dist/confirmDiscardUnsavedComment-D1Q3ha7c.js
--- a/dist/confirmDiscardUnsavedComment-D1Q3ha7c.js
+++ b/dist/confirmDiscardUnsavedComment-D1Q3ha7c.js
@@ -80 +80 @@ var z = (e) => {
-	}), { getFloatingProps: E } = h([C, w]), D = s(""), O = s(null), k = g([O, o.setFloating]);
+	}), { getFloatingProps: E } = h([C, w]), D = s(""), O = s(null), k = g([O, o.setFloating]), P = s(o.setReference), U = s(o.setPositionReference), V = s(), W = s(), X = s(); P.current = o.setReference, U.current = o.setPositionReference;
@@ -84 +84 @@ var z = (e) => {
-			n !== void 0 && (e.focusManagerProps?.disabled || !t.isWithinEditor(n)) && o.setReference(n);
+			n !== void 0 && (e.focusManagerProps?.disabled || !t.isWithinEditor(n)) && V.current !== n && (V.current = n, P.current(n));
@@ -86,5 +86,6 @@ var z = (e) => {
-			o.setPositionReference({
-				getBoundingClientRect: L(e.reference),
-				...r ? { getClientRects: r } : {},
-				contextElement: n
-			});
+			let q = L(e.reference), M = q(), A = r ? Array.from(r(), (F) => `${F.x}:${F.y}:${F.width}:${F.height}`).join(";") : "", G = { element: n, x: M.x, y: M.y, width: M.width, height: M.height, clientRects: A }, H = W.current; X.current = { getBoundingClientRect: q, getClientRects: r };
+			(!H || H.element !== G.element || H.x !== G.x || H.y !== G.y || H.width !== G.width || H.height !== G.height || H.clientRects !== G.clientRects) && (W.current = G, U.current({
+				getBoundingClientRect: () => X.current.getBoundingClientRect(),
+				...r ? { getClientRects: () => X.current.getClientRects() } : {},
+				contextElement: n
+			}));
@@ -94 +95,0 @@ var z = (e) => {
-		o,
diff --git a/src/components/Popovers/GenericPopover.tsx b/src/components/Popovers/GenericPopover.tsx
--- a/src/components/Popovers/GenericPopover.tsx
+++ b/src/components/Popovers/GenericPopover.tsx
@@ -167,0 +168,19 @@ export const GenericPopover = (
+  const setReferenceRef = useRef(refs.setReference);
+  const setPositionReferenceRef = useRef(refs.setPositionReference);
+  const appliedReferenceElementRef = useRef<Element | undefined>(undefined);
+  const appliedPositionReferenceRef = useRef<
+    | {
+        element: Element | undefined;
+        x: number;
+        y: number;
+        width: number;
+        height: number;
+        clientRects: string;
+      }
+    | undefined
+  >(undefined);
+  const positionReferenceSourceRef = useRef<
+    { getBoundingClientRect: () => DOMRect; getClientRects?: () => DOMRectList } | undefined
+  >(undefined);
+  setReferenceRef.current = refs.setReference;
+  setPositionReferenceRef.current = refs.setPositionReference;
@@ -181,13 +200,15 @@ export const GenericPopover = (
       if (
         element !== undefined &&
+        appliedReferenceElementRef.current !== element &&
         (props.focusManagerProps?.disabled || !editor.isWithinEditor(element))
       ) {
         // Only set domReference when FloatingFocusManager is disabled.
         // When FloatingFocusManager is active (disabled !== false) and the
         // reference is inside the ProseMirror editor, setting domReference
         // causes floating-ui to call insertAdjacentElement on the reference,
         // inserting a focus-return <span> into the PM contenteditable. This
         // triggers PM's MutationObserver and resets the editor selection.
         // (issue #2525)
-        refs.setReference(element);
+        appliedReferenceElementRef.current = element;
+        setReferenceRef.current(element);
       }
@@ -196,9 +217,54 @@ export const GenericPopover = (
-      refs.setPositionReference({
-        getBoundingClientRect: getMountedBoundingClientRectCache(
-          props.reference,
-        ),
-        ...(getClientRects ? { getClientRects } : {}),
-        contextElement: element,
-      });
+      const getBoundingClientRect = getMountedBoundingClientRectCache(
+        props.reference,
+      );
+      const rect = getBoundingClientRect();
+      const clientRects = getClientRects
+        ? Array.from(
+            getClientRects(),
+            ({ x, y, width, height }) => `${x}:${y}:${width}:${height}`,
+          ).join(";")
+        : "";
+      const nextPositionReference = {
+        contextElement: element,
+        x: rect.x,
+        y: rect.y,
+        width: rect.width,
+        height: rect.height,
+        clientRects,
+      };
+      const appliedPositionReference = appliedPositionReferenceRef.current;
+      positionReferenceSourceRef.current = {
+        getBoundingClientRect,
+        ...(getClientRects ? { getClientRects } : {}),
+      };
+      if (
+        appliedPositionReference === undefined ||
+        appliedPositionReference.element !== nextPositionReference.contextElement ||
+        appliedPositionReference.x !== nextPositionReference.x ||
+        appliedPositionReference.y !== nextPositionReference.y ||
+        appliedPositionReference.width !== nextPositionReference.width ||
+        appliedPositionReference.height !== nextPositionReference.height ||
+        appliedPositionReference.clientRects !== nextPositionReference.clientRects
+      ) {
+        appliedPositionReferenceRef.current = {
+          element: nextPositionReference.contextElement,
+          ...nextPositionReference,
+        };
+        setPositionReferenceRef.current({
+          getBoundingClientRect: () =>
+            positionReferenceSourceRef.current!.getBoundingClientRect(),
+          ...(getClientRects
+            ? {
+                getClientRects: () =>
+                  positionReferenceSourceRef.current!.getClientRects!(),
+              }
+            : {}),
+          contextElement: element,
+        });
+      }
     }
-  }, [props.reference, refs, props.focusManagerProps?.disabled, editor]);
+  }, [
+    props.reference,
+    props.focusManagerProps?.disabled,
+    editor,
+  ]);
