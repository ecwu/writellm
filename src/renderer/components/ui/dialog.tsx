import { createContext, useContext, useEffect, useId, useRef, useState, type HTMLAttributes, type ReactElement, type ReactNode } from 'react';
import { createPortal } from 'react-dom';
const Context=createContext<{open:boolean;setOpen:(v:boolean)=>void;titleId:string;trigger:HTMLElement|null;setTrigger:(v:HTMLElement|null)=>void}|null>(null);
export function Dialog({children,open:onOpen,onOpenChange}:{children:ReactNode;open?:boolean;onOpenChange?:(v:boolean)=>void}){const [local,setLocal]=useState(false);const [trigger,setTrigger]=useState<HTMLElement|null>(null);const titleId=useId();const open=onOpen??local;const setOpen=(v:boolean)=>{if(onOpen===undefined)setLocal(v);onOpenChange?.(v);if(!v)queueMicrotask(()=>trigger?.isConnected&&trigger.focus());};return <Context.Provider value={{open,setOpen,titleId,trigger,setTrigger}}>{children}</Context.Provider>}
export function DialogTrigger({children}:{children:ReactElement<{onClick?:()=>void}>}){const c=useContext(Context)!;return <span ref={(n)=>c.setTrigger(n?.firstElementChild as HTMLElement|null)} onClick={()=>c.setOpen(true)}>{children}</span>}
export function DialogContent({children,className,...p}:HTMLAttributes<HTMLDivElement>){const c=useContext(Context)!;const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(c.open)queueMicrotask(()=>ref.current?.querySelector<HTMLElement>('button,input,select,textarea,[tabindex]:not([tabindex="-1"])')?.focus());},[c.open]);if(!c.open)return null;return createPortal(<div className="ui-dialog-backdrop" onMouseDown={(e)=>{if(e.target===e.currentTarget)c.setOpen(false)}}><div ref={ref} role="dialog" aria-modal="true" aria-labelledby={c.titleId} className={className??'ui-dialog'} onKeyDown={(e)=>{if(e.key==='Escape')c.setOpen(false);}} {...p}>{children}</div></div>,document.body)}
export function DialogTitle(p:HTMLAttributes<HTMLHeadingElement>){const c=useContext(Context)!;return <h2 id={c.titleId} {...p}/>}; export const DialogDescription=(p:HTMLAttributes<HTMLParagraphElement>)=><p {...p}/>; export function DialogClose({children}:{children:ReactElement}){const c=useContext(Context)!;return <span onClick={()=>c.setOpen(false)}>{children}</span>}
