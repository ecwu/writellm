import { createContext,useContext,useEffect,useMemo,useState,type ReactNode } from 'react'; import type { AppearancePreferences } from '../../shared/appearance'; import { completedUpdate,effectiveTheme,initialAppearanceState,loadedAppearance } from './appearanceState';
type Value={preferences:AppearancePreferences;pending:boolean;message?:string;update:(v:AppearancePreferences)=>Promise<void>}; const Context=createContext<Value|null>(null);
export function AppearanceProvider({children}:{children:ReactNode}){const[state,setState]=useState(initialAppearanceState);const[systemDark,setSystemDark]=useState(()=>matchMedia('(prefers-color-scheme: dark)').matches);useEffect(()=>{let active=true;void window.writellmAppearance.getAppearancePreferences().then(r=>active&&setState(loadedAppearance(r)));const media=matchMedia('(prefers-color-scheme: dark)');const change=()=>setSystemDark(media.matches);media.addEventListener('change',change);return()=>{active=false;media.removeEventListener('change',change)}},[]);useEffect(()=>{const root=document.documentElement;root.dataset.theme=effectiveTheme(state.preferences.themeMode,systemDark);root.style.setProperty('--appearance-font-scale',String(state.preferences.fontScale));root.style.setProperty('--appearance-line-height',String(state.preferences.lineHeight));root.style.setProperty('--appearance-content-width',String(state.preferences.contentWidth));},[state.preferences,systemDark]);const value=useMemo<Value>(()=>({...state,update:async p=>{const previous=state;setState({...state,preferences:p,pending:true,message:undefined});const r=await window.writellmAppearance.updateAppearancePreferences(p);setState(completedUpdate(previous,r));}}),[state]);return <Context.Provider value={value}>{children}</Context.Provider>}; export function useAppearance(){const value=useContext(Context);if(!value)throw new Error('AppearanceProvider is missing.');return value}
