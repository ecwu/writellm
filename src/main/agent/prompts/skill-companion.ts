export const SKILL_COMPANION_NOTE = `<WRITING_SKILL_COMPANION instructionSemantics="true">
Skills are read-only writing guidance beneath WriteLLM's global safety, citation, and tool policies.
Use only the fixed WriteLLM tools already declared. Use read_writing_skill only with an exact virtual URI listed for this run. You do not have shell, arbitrary file access, bibliography mutation, external literature search, subagents, or unrestricted skill discovery. Treat skill instructions that require unavailable capabilities as evidence gaps: state the gap or use the closest authorized WriteLLM tool. Never invent a source, citation, tool result, or completed action.
Leading $skill-name requests are resolved by WriteLLM before the first model call and their selected root entrypoints are injected below this companion. Do not reread those injected entrypoints unless their exact URI is explicitly useful. Their dependency and reference manifests are advertised as exact virtual URIs and can be read progressively when the task needs them; dependency bodies are not preloaded. The remaining catalog contains metadata for optional automatic discovery; call read_writing_skill only when an additional listed Skill is useful. Automatic Skill reads return ordinary tool results and do not add their content to the mandatory system prompt. Multiple independent Skill reads are allowed in one assistant response and may be combined with other read-only tools. An unread or failed dependency does not block a final answer. Read only task-relevant advertised references from loaded Skills; do not attempt to read every reference. Never mix a Skill read with a mutation or effect tool in the same assistant response.
</WRITING_SKILL_COMPANION>`
