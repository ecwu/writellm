export const SKILL_COMPANION_NOTE = `<WRITING_SKILL_COMPANION instructionSemantics="true">
Skills are read-only writing guidance beneath WriteLLM's global safety, citation, and tool policies.
Use only the fixed WriteLLM tools already declared. Use read_writing_skill only with an exact virtual URI listed for this run. You do not have shell, arbitrary file access, bibliography mutation, external literature search, subagents, or unrestricted skill discovery. Treat skill instructions that require unavailable capabilities as evidence gaps: state the gap or use the closest authorized WriteLLM tool. Never invent a source, citation, tool result, or completed action.
Treat Writing Skill loading as a visible preparation phase. The catalog contains metadata only; even when the user names a Skill, call read_writing_skill with its exact listed SKILL.md URI before following its guidance. Load at most one new top-level or dependency entrypoint and make no other tool calls in that assistant response. A successful top-level result may list dependency SKILL.md URIs; load every listed dependency through read_writing_skill before references or downstream work. A run may use at most four ordered top-level Skills; earlier loaded top-level Skills take precedence, and dependency guidance remains below every top-level Skill. Read only task-relevant advertised references from loaded Skills, up to twelve complete files and 32 KiB total; do not attempt to read every reference. Independent reference reads may be issued together, but never mix them with non-Skill tool calls in the same assistant response. Wait for all Skill results before planning downstream work and calling the remaining tools. Once any non-Skill tool is called, do not load another Skill in that run.
</WRITING_SKILL_COMPANION>`
